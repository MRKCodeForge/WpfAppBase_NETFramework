<Window x:Class="WpfAppBaseDotNetFramework.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfAppBaseDotNetFramework.Views"
        xmlns:vm="clr-namespace:WpfAppBaseDotNetFramework.ViewModels"
        mc:Ignorable="d"
        d:DataContext="{d:DesignInstance vm:MainWindow_VM, IsDesignTimeCreatable=False}"
        Title="MainWindow"
        Height="450"
        Width="800"
        DataContext="{Binding Source={StaticResource Locator}, Path=MainWindow_VM}">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto" />
            <RowDefinition Height="auto" />
            <RowDefinition Height="auto" />
            <RowDefinition />
        </Grid.RowDefinitions>

        <StackPanel>

            <TextBlock VerticalAlignment="Center"
                       HorizontalAlignment="Center"
                       Text="Text con stile globale" />

            <TextBlock VerticalAlignment="Center"
                       HorizontalAlignment="Center"
                       Style="{StaticResource BoldItalicTextBlockStyle}"
                       Text="Text con stile specifico" />

        </StackPanel>

        <Grid Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="auto" />
                <RowDefinition Height="auto" />
                <RowDefinition Height="auto" />
                <RowDefinition />
            </Grid.RowDefinitions>

            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="auto" />
                    <ColumnDefinition />
                    <ColumnDefinition />
                    <ColumnDefinition />
                </Grid.ColumnDefinitions>

                <Button x:Name="ChangeButton"
                        Width="80"
                        Content="Change"
                        Command="{Binding ChangeStateCommand}" />

                <Ellipse Grid.Column="1"
                         Height="20"
                         Width="20"
                         StrokeThickness="1"
                         Stroke="Black"
                         Fill="{Binding State, Converter={StaticResource BoolToColorConverter}}" />

                <Ellipse Grid.Column="2"
                         Height="20"
                         Width="20"
                         StrokeThickness="1"
                         Stroke="Black"
                         Fill="{Binding State, Converter={StaticResource BoolToColorConverter}}"
                         Visibility="{Binding State, Converter={StaticResource BoolToVisibilityConverter}}" />

                <Ellipse Grid.Column="3"
                         Height="20"
                         Width="20"
                         StrokeThickness="1"
                         Stroke="Black"
                         Fill="{Binding State, Converter={StaticResource BoolToColorConverter}}"
                         Visibility="{Binding State, Converter={StaticResource BoolToVisibilityConverter}, ConverterParameter=INVERT}" />

            </Grid>

            <Grid Grid.Row="1"
                  Margin="0 5 0 0">

                <CheckBox Content="Posso cambiare?"
                          IsChecked="{Binding CanChange}" />

            </Grid>

            <Button Grid.Row="2"
                    Height="30"
                    Click="Button_Click" />

        </Grid>

    </Grid>
</Window>
